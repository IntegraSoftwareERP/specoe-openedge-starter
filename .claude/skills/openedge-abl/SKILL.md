---
name: openedge-abl
description: Referencia general del lenguaje ABL de Progress OpenEdge. Tipos, OOP, procedures, statements (FOR EACH, FIND, CAN-FIND), ProDataSets, JSON, funciones de string, .NET interop. Usar para preguntas sobre ABL, sintaxis, tipos, o referencias del framework.
---

# OpenEdge ABL — Referencia

Skill **libre** — incluido completo en el starter.

Cubre:

- **Tipos**: CHARACTER, INTEGER, DECIMAL, LOGICAL, LONGCHAR, MEMPTR, ROWID, HANDLE, DATETIME-TZ, INT64
- **OOP**: CLASS, INTERFACE, ENUM, INHERITS, IMPLEMENTS, PROPERTY con GET/SET
- **Statements**: DEFINE TEMP-TABLE, DEFINE DATASET, FOR EACH, FIND, CAN-FIND, REPEAT, CASE, IF-THEN-ELSE
- **Error handling**: BLOCK-LEVEL ON ERROR UNDO, THROW, CATCH, FINALLY
- **ProDataSets**: DATA-RELATION, FILL, TRACK-CHANGES, MERGE-CHANGES
- **JSON**: WRITE-JSON, READ-JSON, JsonObject, JsonArray
- **Strings**: SUBSTRING, INDEX, REPLACE, TRIM, LENGTH, NUM-ENTRIES
- **.NET interop**: USING, INPUT-OUTPUT parameters

> Por ahora este skill es un stub del starter. La referencia completa con ejemplos
> se incluira cuando el MCP Skill Server tenga el contenido indexado.
>
> Para documentacion oficial Progress:
> https://docs.progress.com/bundle/openedge-abl-reference/
