node_modules
dist
.env
.env.local
.env.production
*.log
.DS_Store
.vscode/settings.json
.idea/
coverage/
# Archivos generados por el starter al renderizar
.rendered/
# Licencia local (no commitear)
.claude/specoe-license-cache.json
.claude/integra-hub-session.json
# Bootstrap del room: lo genera setup.sh en runtime y lleva el JWT del skill-server inline
# (specoe-license-check.mjs lo reescribe con el JWT fresco). Nunca se commitea. Ademas, sin
# esta linea quedaba untracked y reportado por git status, lo que hacia imposible cualquier
# criterio de divergencia acotada sobre la carpeta del room (SPEC-0167 P3, ADR-004).
.mcp.json
# Marca del corte del check de config: la escribe setup.sh con los campos que faltan editar y la
# borra el paso que pasa (TKT-0307). Es estado local de la carpeta, y sin esta linea quedaria
# untracked frenando la segunda pasada por el criterio de divergencia acotada de arriba.
.specoe-config-pending
